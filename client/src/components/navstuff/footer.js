import React from "react";

export default function Footer(props) {
    return(
        <footer class="footer">
            <div class="container">
                This is a footer.
            </div>
        </footer>
    );
}